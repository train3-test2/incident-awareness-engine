# R1 Scenario — Remote Administration Pairs

> **상태: 초안(Draft).** 역할 4가 소유하며, 역할 2·3·5의 검토를 받아 확정한다.
>
> 상위 정본은 `통합 표준안 3.0` §7-2, 데이터 계약은 `docs/data-contract-v0.2.md` 를 따른다.
> 이 문서는 R1 의 행위·Pair 설계·Ground Truth 규칙만 정의하며, 평가식이나 Contract 필드를
> 재정의하지 않는다.
>
> **현재 범위는 R1-V02 한 가지다.** 다른 R1 Pair 는 설계가 확정되는 대로 절을 추가한다.

---

## 1. R1 공통 전제

issue #72 에 정리된 값 가운데 R1-V02 에 필요한 것만 옮긴다. 출처 문서(통합 표준안, PoC
가이드)는 저장소 밖에 있다.

| 항목 | 값 |
| --- | --- |
| `gate_type` | `feasibility` |
| `ambiguity_required` | `true` — 초기 telemetry 중첩 필수 |
| `expected_overlap` | `powershell_execution`, `remote_session` |
| `expected_divergence` | `credential_access`, `lateral_movement`, `abnormal_process_lineage` |
| `entity_id` | Endpoint Host. Evidence 는 Source Event 가 관측된 host 에 귀속 (D-01, `data-contract-v0.2.md` §5-4) |
| host 구성 | Controller → Target-A. 필요하면 내부 연결 대상 Target-B |
| 네트워크 | Internal / Host-only. **인터넷 연결 없음** |

**S0 의 외부 연결 승인과 무관하다.** S0 는 issue #71 에서 제한된 NAT 예외(방식 A)를 다루지만,
R1-V02 는 내부망 안에서만 동작하므로 그 승인값을 쓰지 않는다.

### 하지 않는 것

```text
현재 단계에서 R1-V02 결과로 탐지 성능을 주장하지 않는다
성능 수치(탐지율, TTSD, FPR 등)를 이 문서에 적지 않는다
계보 Evidence 가 정의되기 전에는 Evidence·Score 경로를 검증 대상으로 삼지 않는다
```

**현재 단계에서 할 수 있는 것은 §8-2 의 telemetry Pilot 까지다** — 원본 Sysmon JSONL 로 연결과
계보를 검증한다. 같은 검증을 정규화 경로로 재현하는 §8-3 Full 은 ProcessGuid 계약이 확장되기
전까지 `blocked` 이다. Pilot 통과는 Full 성공이나 탐지 성능 검증이 아니고, Full 통과도 R1 비교
평가 진입이 아니다(§8-4). 이유는 §8 과 §10 에 있다.

### 평가 가설과 주장 범위

**가설** — 개별 행위만으로 정상과 공격을 구분하기 어려운 조건에서, Evidence 의 시간 관계와 프로세스
계보를 결합하면 정상 false alert 를 억제하면서 침해사고 인지 시점을 앞당길 수 있는가.

- 기존 EDR/XDR 이 단일 이벤트만 본다고 주장하지 않는다
- 상용 EDR 을 같은 입력 · 관측 범위로 직접 비교하지 않았다면 EDR 보다 우수하다고 주장하지 않는다
- 기본 주장은 사전에 동결한 룰 기반 baseline 대비 개선 여부로 제한한다
- SASE · EDR · IPS · SEP 를 대체하지 않는다. 기존 탐지와 원본 이벤트를 연결하는 인지 · 근거 계층으로
  평가한다

---

## 2. R1-V02 원격 프로세스 계보 — 목적과 범위

| 항목 | 값 |
| --- | --- |
| `scenario_id` | `R1` |
| `variation_id` | `V02` |
| 분기 축 | `abnormal_process_lineage` |
| 관측 host | **Target-A 하나** |
| 필요한 Sysmon EventID | 1, 3 |
| 추가 로그 채널 | 없음 |
| 규모 | Normal 1회 + Attack 1회 (R1 Pilot 수집 단계) |

**검증 목적** — 새 로그 채널 없이 **프로세스 계보와 연결 주체**의 차이가 telemetry 로 관측되는지
확인한다. 두 Run 은 원격 세션 · 최종 Image · 목적지 · 포트 · 관측 범위를 같게 둔다.

```text
Normal   원격 세션 host  ->  승인된 wrapper 또는 자동화 프로세스  ->  최종 관리 도구
Attack   원격 세션 host  ->  승인 경로와 다른 중간 프로세스      ->  같은 최종 관리 도구
```

**판정 원칙** — 중간 프로세스 존재나 계보 깊이만으로 Attack 을 판정하지 않는다. 라벨은 행위 목적 ·
인가 범위 · 실행 기록 · Ground Truth 를 함께 근거로 한다. ProcessGuid 는 계보 체인을 재구성하는 데
쓰며, 계보 깊이는 단독 정답 조건이 아니다.

**이 Pair 를 먼저 다루는 이유**

- 현재 Sysmon 설정(`configs/sysmon/sysmonconfig-sample-v0.1.xml`)이 기록하는 EID 1·3 만 쓴다
- 현재 normalizer 가 처리하는 `process_create` · `network_connection` 만 쓴다
- 관측 host 가 하나라 `RunMetadata.target_host` 단일값과 맞고, 다중 host 축약 규칙이 필요 없다

---

## 3. Target-A 기준 관측 구조

```text
Controller                         Target-A (관측 host)                          Target-B
    |                                   |                                           |
    |--- t+0 원격 세션 수립 (WinRM) --->| 원격 세션 host 프로세스 생성               |
    |                                   | t+2 공통 준비 (두 Run 동일)                |
    |                                   | t+5 Normal : host -> 승인 wrapper -> 최종 관리 도구 |
    |                                   | t+5 Attack : host -> 다른 중간 -> 최종 관리 도구    |
    |                                   |     (최종 관리 도구 Image 는 두 Run 동일)   |
    |                                   |--- t+8 t+5 관리 도구 인스턴스가 연결 ----->|
```

- Evidence 는 Target-A 의 Source Event 에만 귀속한다
- Controller 와 Target-B 에서 telemetry 를 수집하더라도 **별도 entity** 로 다루고 Target-A 와 합치지 않는다
- 원격 세션 host 프로세스 이름은 `wsmprovhost.exe` 로 **가정**하며, 실제 이름은 rehearsal telemetry 로 확인한다
- WinRM 목적 포트는 `TBD` (§11)

---

## 4. 행위 목록

`t+분` 은 설계상 상대 시간이다. 실제 기록은 `execution_record` 의 실행 시각이 정본이다.

### 4-1. 공통 구간과 최초 분기

| 시각 | Normal | Attack | 성격 |
| --- | --- | --- | --- |
| t+0 | Controller → Target-A 원격 세션 수립 | 동일 | **공통 준비** |
| t+2 | 원격 세션에서 결과 저장 위치 준비와 로컬 파일 기록 (부수 행위) | 동일 | **공통 준비** |
| t+5 | 원격 세션 host → **승인된 wrapper 또는 자동화 프로세스** → 최종 관리 도구 — ProcessCreate | 원격 세션 host → **승인 경로와 다른 중간 프로세스** → **같은 최종 관리 도구** — ProcessCreate | **최초 분기** |
| t+8 | t+5 에 생성된 최종 관리 도구 인스턴스가 승인 목적지로 내부 연결 | 동일 (t+5 인스턴스가 같은 목적지 · 포트로 연결) | 분기(연결 주체의 계보) |
| t+10 | 세션 종료 | 세션 종료 | 공통 |

**t+0 ~ t+5 직전은 공통 준비 구간이다.** 두 Run 은 같은 원격 세션에서 같은 준비 작업을 한다.
t+2 의 파일 기록은 부수 행위이며 분기로 쓰지 않는다.

**최초 분기는 t+5 이고, 두 Run 모두 t+5 에 ProcessCreate 가 발생한다.** 최종 관리 도구의
Image 는 두 Run 에서 같다. 두 Run 모두 중간 단계를 거치며, 다른 것은 **그 최종 프로세스 인스턴스의
부모 계보가 승인 경로인지와 인가 문맥**이다.

**Normal 도 t+8 에 내부 연결을 만든다.** 두 Run 의 연결은 같은 Image, 같은 목적지, 같은 포트다.
차이는 연결을 만든 인스턴스의 ProcessGuid 와 그 부모 계보에 있다. 연결 유무나 Image 가 답이
되면 hard negative 가 성립하지 않는다.

관리 도구 · wrapper · 중간 프로세스는 **무해한 승인 도구**로 한정하며 구체 선정은 `TBD` 다. 이 문서는
명령·도구 이름·페이로드를 적지 않는다.

### 4-2. reference action

최초 공통 행위(t+0 원격 세션 수립)를 기준으로 하는 것을 제안한다. `reference_time` 은 그
행위가 Target-A 에 남긴 최초 telemetry 시각이다. 확정은 `TBD` (§11).

---

## 5. 예상 telemetry

### 5-1. Sysmon EventID

| host | EventID | 기대 내용 | 현재 Sysmon 설정으로 나오는가 |
| --- | --- | --- | --- |
| Target-A | **1** ProcessCreate | t+5 두 Run 모두 최종 관리 도구 생성. **Image 동일**, 부모만 다름(Normal: 승인 wrapper, Attack: 승인 경로와 다른 중간 프로세스). 두 Run 모두 중간 단계의 EID 1 이 하나 더 있다 | 예 |
| Target-A | **3** NetworkConnect | t+8 두 Run 모두 t+5 최종 관리 도구 인스턴스가 연결. **Image · 목적지 · 포트 동일**, 연결 인스턴스의 **ProcessGuid 와 그 부모 계보가 다름** | 예 |

추가 Sysmon 설정 변경은 필요 없다.

### 5-2. 현재 계약으로 확인할 수 있는 것 — `parent_name`

`normalize_sysmon_process_create` 는 EID 1 의 `ParentImage` 를 `process.parent_name` 으로
옮긴다(`src/incident_awareness/normalization/sysmon.py`). 따라서 **t+5 최종 관리 도구의 직접 부모
이름은 현재 event_v0 로 볼 수 있다.**

```text
Normal   t+5 최종 관리 도구  process.name = <관리 도구>  process.parent_name = <승인 wrapper>
Attack   t+5 최종 관리 도구  process.name = <관리 도구>  process.parent_name = <중간 프로세스>
```

`process.name` 은 두 Run 에서 같다. `parent_name` 은 다르지만 부모 이름 하나로 판정하지 않는다. 이름만
으로 갈리면 shortcut 이 된다(§7).

### 5-3. 현재 계약으로 확인할 수 없는 것 — 계보 깊이와 EID 1 → 3 연결

`ProcessInfo`(`src/incident_awareness/common/models/event.py`)는 `pid`, `name`, `path`,
`command_line`, `parent_pid`, `parent_name` 만 갖고 `extra="forbid"` 다. ProcessGuid 계열
필드가 없다.

| 확인하려는 것 | 필요한 연결 | 현재 |
| --- | --- | --- |
| 계보 체인 재구성 | 조부모 이상의 체인 | **불가** — 한 단계 부모 이름만 표현된다 |
| t+8 연결을 만든 프로세스의 계보 | EID 3 과 EID 1 의 결합 | **불가** — 아래 참조 |

Sysmon EID 3 원천에는 `ParentImage` 가 없다(`samples/raw/sysmon-0001.jsonl` 로 확인). 그래서
정규화된 `network_connection` 의 `process.parent_name` 은 항상 `null` 이다. 연결 주체의 계보를
알려면 EID 3 의 `ProcessGuid` 로 EID 1 을 찾아야 하는데, 그 값이 event_v0 에 남지 않는다.
`parent_pid` 로 잇는 방법은 PID 가 재사용될 수 있어 신뢰하지 않는다.

**결과적으로 정규화 경로(event_v0)에서는 한 단계 부모 이름까지만 보인다.** Normal 에도 승인 wrapper
가 있으므로 이것만으로는 판정 근거가 되지 않는다.

이 제한은 정규화 경로에만 해당한다. 원본 Sysmon JSONL 에는 `ProcessGuid` · `ParentProcessGuid` 가
남아 있으므로, 원본을 쓰는 §8-2 Pilot 은 EID 1 ↔ EID 3 연결과 전체 계보를 검증할 수 있다.

---

## 6. Ground Truth

`data-contract-v0.2.md` 의 RunMetadata · ExecutionRecord 경계를 따른다. R1-V02 에서 추가로
남길 항목은 다음과 같다.

| 항목 | 기록 위치 |
| --- | --- |
| `scenario_id` · `variation_id` · `run_id` · `run_type` · 반복 번호 | RunMetadata |
| 행위별 실행 시각 | `execution_record.csv` |
| reference action 과 `reference_time` · `reference_source_event_id` | RunMetadata |
| 최초 분기 action | 이 문서의 행위 표 |
| 단계별 프로세스 이름 · 경로 · ProcessGuid · ParentProcessGuid | 실행 기록(원천 telemetry 에서 추출) |
| t+5 최종 관리 도구 인스턴스의 ProcessGuid 와 Image | 실행 기록 |
| 단계별 계보 체인 (깊이는 라벨 근거가 아님) | 실행 기록 |
| 행위 목적 · 인가 범위 (Attack 라벨의 근거) | 실행 기록 |
| t+8 연결의 ProcessGuid · 목적지 IP · 포트 · 시각 · host | 실행 기록 |
| 승인된 목적지 IP · 포트와 t+5 · t+8 판정 시간 구간 | 이 문서 §11 (`TBD`) |
| Sysmon config version · hash | RunMetadata · Manifest |
| snapshot 이름과 복원 확인 | RunMetadata(`vm_snapshot`) · 실행 기록 |

`ExecutionRecordRow` 에는 host 필드가 없다(issue #72 질문 7). R1-V02 는 관측 host 가
Target-A 하나이므로 행위 표와 `action_id` 로 충분하다고 보고 ExecutionRecord 계약을 바꾸지 않는다.

ProcessGuid 는 event_v0 에 들어오지 않으므로 **원천 telemetry 에서 뽑아 실행 기록으로만**
보관한다. Ground Truth 는 런타임 입력이 아니므로 이 값이 Evidence 나 Fusion 에 흘러가지 않는다.

---

## 7. hard negative 와 변형 축

**Normal 은 idle 이 아니다.** 같은 원격 세션을 열고, t+5 에 같은 최종 관리 도구를 실행하고,
t+8 에 같은 목적지 · 포트로 내부 연결까지 만든다. Normal 도 승인 wrapper 를 거치므로 중간 단계의
존재는 두 Run 에 공통이다. Attack 과 다른 것은 최종 관리 도구 인스턴스의 부모 계보가 승인 경로인지와
인가 문맥이다.

| 축 | 변형 | 비고 |
| --- | --- | --- |
| 계보 깊이 | 3단계 / 4단계 | Normal 도 승인 wrapper 를 거치므로 깊이 자체는 정답이 되지 않는다 |
| 중간 프로세스 종류 | Normal 승인 wrapper · 자동화 / Attack 스크립트 해석기 등 | 구체 선정 `TBD` |
| 분기 시각 | t+5 / t+9 | Fusion window 보다 충분히 넓게 |
| 연결 대상 | Target-B / 내부 파일 서버 | 실험망 CIDR 필수(`TBD`) |

**Shortcut 통제** — 프로세스·파일 이름에 `attack_`·`normal_` 같은 prefix 를 두지 않는다. 계정,
작업 디렉터리, 행위 수는 두 Run 에서 같게 한다.

---

## 8. 성공 · 실패 판정

**이 판정은 telemetry 수집 품질에 대한 것이다.** 탐지 성능 판정이 아니다.

**t+8 에 EID 3 이 있다는 사실만으로는 어느 단계에서도 통과로 보지 않는다.** 두 Run 모두 같은
Image 로 같은 목적지에 연결하므로, EID 3 의 존재는 Normal 과 Attack 을 구분하지 못한다.

### 8-1. 검증 조건

두 Run 각각에 대해 아래를 **모두** 만족해야 한다. 이 조건은 §8-2 Pilot 과 §8-3 Full 에 똑같이
적용되며, 두 단계는 **어떤 데이터 경로로 확인하느냐**가 다르다. Full 은 여기에 계보 Evidence 의
연결 경로 확인이 더해진다(§8-3).

| # | 조건 |
| --- | --- |
| S-1 | 공통 준비 구간(t+0 ~ t+5 직전)에서 두 Run 의 관측 EventID 종류와 프로세스 계보가 같다 |
| S-2 | t+5 판정 구간에 **지정 최종 관리 도구의 EID 1** 이 있고, 두 Run 의 Image 가 같다. 이 EID 1 의 ProcessGuid 를 기준값으로 삼는다 |
| S-3 | 그 EID 1 의 부모 계보가 Run 설계와 맞다 — Normal 은 원격 세션 host → 승인 wrapper → 최종 도구, Attack 은 원격 세션 host → 승인 경로와 다른 중간 프로세스 → 최종 도구. 중간 단계 존재나 깊이만으로 판정하지 않는다 |
| S-4 | **S-2 의 ProcessGuid 와 같은 ProcessGuid 를 가진 EID 3** 이 있다 |
| S-5 | 그 EID 3 의 목적지 IP 가 승인된 값이다 (`TBD`, §11) |
| S-6 | 그 EID 3 의 목적지 포트가 승인된 값이다 (`TBD`, §11) |
| S-7 | 그 EID 3 의 시각이 지정된 t+8 판정 구간 안이다 (`TBD`, §11) |
| S-8 | S-2 의 EID 1 과 S-4 의 EID 3 이 모두 예상 host(Target-A)에서 기록됐다 |

판정 단계와 현재 상태는 다음과 같다.

| 단계 | 데이터 경로 | 현재 상태 | 결과 표기 |
| --- | --- | --- | --- |
| **telemetry Pilot** (§8-2) | Target-A 원본 Sysmon JSONL | 실행 가능 | `pass` / `fail` |
| **Full** (§8-3) | NormalizedEvent → Evidence | **`blocked`** | `not_evaluated` |

R1 비교 평가는 이 판정 단계에 들어 있지 않다. 진입 조건은 §8-4 에 따로 둔다.

### 8-2. telemetry Pilot — 원본 JSONL 로 연결 · 계보 검증

원본 Sysmon JSONL 은 ProcessGuid 를 그대로 갖고 있다. 그래서 Pilot 은 필드가 있는지만 보지 않고
**실제 연결이 일치하는지까지** 원본 레코드로 검증한다.

**확인 절차**

1. t+5 판정 구간에서 지정 최종 관리 도구의 EID 1 을 찾고 그 `ProcessGuid` 를 기준값으로 삼는다
2. t+8 판정 구간에서 **그 ProcessGuid 와 같은 `ProcessGuid` 를 가진 EID 3** 을 찾는다
3. 그 EID 3 의 `DestinationIp` · `DestinationPort` · 시각 · `Computer` 를 승인값과 비교한다
4. 두 Run 의 t+5 최종 관리 도구 `Image` 를 비교한다
5. 원본 EID 1 의 `ParentProcessGuid` 를 따라 올라가 두 Run 의 부모 계보를 비교한다

**Pilot 통과** — 두 Run 모두 §8-1 의 S-1 ~ S-8 을 원본 JSONL 에서 만족한다. 즉 아래가 모두 참이다.

| 확인 | 대응 조건 |
| --- | --- |
| t+5 지정 최종 관리 도구의 EID 1 이 있고 그 `ProcessGuid` 가 있다 | S-2 |
| Normal 과 Attack 의 최종 `Image` 가 같다 | S-2 |
| 두 Run 의 부모 계보 차이가 원본 EID 1 체인에서 확인된다 | S-3 |
| t+8 판정 구간에 EID 3 이 있고 그 `ProcessGuid` 가 t+5 EID 1 과 같다 | S-4 |
| 목적지 IP · 포트가 승인값과 같다 | S-5 · S-6 |
| 시각이 허용된 t+8 구간 안이다 | S-7 |
| EID 1 과 EID 3 이 모두 Target-A 에서 기록됐다 | S-8 |
| 공통 준비 구간의 EventID 종류와 계보가 두 Run 에서 같다 | S-1 |

**Pilot 실패** — 아래 중 하나라도 해당하면 실패다.

- t+5 지정 최종 관리 도구의 EID 1 이 없거나 `ProcessGuid` 가 없다
- 두 Run 의 최종 `Image` 가 다르다
- 부모 계보 체인이 중간에 끊겼거나, Normal 과 Attack 의 계보가 같다
- t+8 구간의 EID 3 이 **다른 프로세스의 것**이다 — `ProcessGuid` 가 t+5 EID 1 과 다르다
- EID 3 의 목적지 IP · 포트 · 시각 · host 중 하나라도 승인값과 다르다
- 공통 준비 구간의 EventID 종류나 계보가 두 Run 에서 다르다

**Pilot 통과는 Full 성공이 아니며 탐지 성능 검증도 아니다.** 원본 telemetry 에 판정 근거가 실제로
남는다는 것만 확인한다. 이 결과로 R1-V02 의 성공이나 성능을 보고하지 않는다.

### 8-3. Full — 정규화 경로로 telemetry · 계보 검증을 재현

Full 은 §8-2 와 **같은 연결 · 계보 검증을** 원본 JSONL 이 아니라 정규화 · Evidence 경로로 재현하는
단계다. **Full 은 telemetry · 계보 검증이며 R1 비교 평가가 아니다.**

```text
NormalizedEvent  ->  Evidence
```

Full 에서 확인하는 것은 다음과 같다.

| 확인 | 대응 조건 |
| --- | --- |
| 정규화된 이벤트에 ProcessGuid 가 보존된다 | S-2 · S-4 의 전제 |
| 정규화 경로에서 EID 1 과 EID 3 이 같은 ProcessGuid 로 연결된다 | S-4 |
| 정규화 경로에서 부모 계보를 재구성할 수 있다 | S-3 |
| 계보 Evidence 가 생성되고, `Evidence.event_ids -> NormalizedEvent.event_id -> raw_ref` 연결 경로가 유지된다 | `data-contract-v0.2.md` §5-2 |

**현재 상태는 `blocked` 이고 결과는 `not_evaluated` 로 표기한다.** event_v0 에 ProcessGuid 가 없어
(§5-3) S-4 의 EID 1 ↔ EID 3 결합과 S-3 의 전체 계보를 정규화 경로에서 재현할 수 없다. 계보
분기를 나타낼 `evidence_type` 도 아직 없다(§10).

| 구분 | 기준 |
| --- | --- |
| 해제 조건 | ProcessGuid 이벤트 계약 확장, 계보 `evidence_type` 정의 (§10) |
| 통과 | 정규화 경로에서 S-1 ~ S-8 을 재현하고 계보 Evidence 의 연결 경로가 유지되며, 그 결과가 같은 Run 의 Pilot 결과와 일치한다 |
| 실패 | 정규화 경로에서 S-1 ~ S-8 중 하나라도 재현되지 않거나, 계보 Evidence 의 연결 경로가 끊기거나, Pilot 결과와 다르다 |

**Full 통과는 R1 비교 평가 진입이 아니다.** 비교 평가는 §8-4 의 조건이 따로 갖춰져야 한다.

### 8-4. R1 비교 평가 — 진입 조건과 설계

Full 과 별개로, 아래가 **모두** 갖춰진 뒤에만 R1 비교 평가를 시작한다. 이 문서는 조건만 적고
평가식이나 설정값을 정하지 않는다.

| 조건 | 내용 | 근거 · 확인 대상 |
| --- | --- | --- |
| 결정적 비교기 | decisive comparator 를 확정하거나 잠정 동결한다 | 역할 5 — issue #44 후보 초안, #87 단계별 동결 (issue #72) |
| 병렬 실행 | `parallel_required=true` 실행 Config 를 적용한다 | `data-contract-v0.2.md` §8-1 — 실행 Config 는 역할 3 이 확정하고 Fast Comparator 요구 여부는 역할 5 와 확인 |
| 평가 구간 | decisive detector 가 발생하기 전 구간을 평가 범위에 포함한다 | 역할 5 (평가 horizon · 구간). 행위 순서는 이 문서 §4 (issue #72 질문 3) |
| 동결 후 비교 | 평가 설정과 판정 기준을 동결한 뒤 비교를 수행한다 | 역할 5 |

Fusion 을 실행하려면 이와 별도로 §10 의 R1 Fusion profile 과 Fusion 실행 조건(역할 1)이 필요하다.

**비교군** — 같은 정식 Run 산출물로, 입력과 관측 범위를 같게 두고 비교한다.

| # | 비교군 | 비고 |
| --- | --- | --- |
| 1 | 단일 Evidence 규칙 | 사전에 동결한 룰 기반 baseline |
| 2 | 복수 Evidence 단순 합산 | 시간 · 계보 관계를 쓰지 않음 |
| 3 | 시간 · 계보를 사용하는 Fusion | 검증 대상 |
| 4 | 특정 EDR 제품 | 같은 조건으로 실행할 수 있을 때만. 아니면 참고 결과로 분리 |

**ablation** — 3번에서 시간 조건 제거 / 계보 조건 제거 / 둘 다 제거. 2번과 3번의 차이로 Evidence
수가 늘어난 효과와 관계 분석의 효과를 구분한다.

**비교 조건**

- Attack 에만 센서를 더 켜거나 관측을 늘리지 않는다
- 정상 false alert, 공격 탐지와 미탐, 인지 지연을 함께 본다
- 원본 이벤트 시각과 경보를 이용할 수 있게 된 시각을 구분해 기록한다
- 기준 시각 · 임계값 · 비교 규칙은 평가 전에 고정하고, 기존 방식이 잘 탐지하는 사례도 포함한다

comparator 동결 절차는 이 문서에 옮기지 않는다. `docs/roles/role5-fast-detector-policy.md` 의 동결
lifecycle 과 issue #87 을 따른다.

**R1-V02 평가 필수 항목** — 가설과 반증 기준이며 결과를 미리 단정하지 않는다.

| 항목 | 내용 |
| --- | --- |
| 기존 방식의 예상 한계 | 같은 원격 세션 · 최종 Image · 목적지 · 포트 때문에 단일 이벤트나 도구명 규칙만으로는 구분하기 어려울 수 있다 |
| 개선 가설 | t+5 ProcessCreate 의 부모 계보와 같은 ProcessGuid 의 t+8 연결을 시간 관계로 결합하면 인가 문맥 차이를 보완할 수 있다 |
| 정상 대조군 | 승인 wrapper · 자동화 경로를 거쳐 같은 최종 관리 도구로 같은 목적지 · 포트에 연결하는 정상 작업 |
| 반증 조건 | 단순 합산(2번) 대비 시간 · 계보 Fusion(3번)이 정상 false alert 나 인지 지연을 개선하지 못하면 차별화 가설은 지지되지 않는다. 계보 깊이만으로 같은 결과를 얻으면 시간 · 계보 결합의 추가 가치는 입증되지 않는다 |

**Pilot 범위는 바꾸지 않는다.** Pilot 은 §8-2 의 원본 Sysmon JSONL 을 이용한 telemetry · 인과관계
검증이며, Fast 후보 구현이나 평가용 로그 수집을 포함하지 않는다.

### 8-5. 무효

두 단계 모두 아래에 해당하면 판정하지 않는다.

| 기준 |
| --- |
| snapshot 미복원 상태 실행 |
| 같은 `run_id` 재사용 |
| 승인되지 않은 도구 사용 |

---

## 9. 안전 조건

- 격리된 Controller · Target VM 과 복원 가능한 snapshot 에서만 실행한다
- Internal / Host-only 네트워크만 쓴다. 공인 인터넷 연결은 없다
- 실제 운영 계정 · 운영 호스트를 쓰지 않는다
- 관리 도구 · wrapper · 중간 프로세스는 무해한 승인 도구로 한정한다
- 첫 Pilot 은 자격증명 탈취, 메모리 덤프, 로그 삭제, 실제 악성 페이로드, 승인되지 않은 외부 연결을
  포함하지 않는다. 권한 변경도 포함하지 않는다
- 이런 행위가 필요하면 별도 승인된 Extended 단계 후보로만 기록한다. 다른 R1 variation(V01 · V04 등)
  설계는 버리지 않고 후속 후보로 둔다
- 추가 승인은 필요 없다. 다만 도구 선정(`TBD`)은 실행 전에 확정한다

---

## 10. 구현 의존성

| 의존 | 내용 | 확인 대상 |
| --- | --- | --- |
| 계보 Evidence | 계보 분기를 나타낼 `evidence_type` 과 필수 필드가 없다. `configs/evidence_types_v0.2.yaml` 은 S0 용 2종뿐 | 역할 2 — issue #85 |
| ProcessGuid 계약 확장 | 계보 체인 재구성과 EID 1 → 3 연결에 필요. `ProcessInfo` 에 필드를 추가하면 `data-contract-v0.2.md` §2 에 따라 NormalizedEvent 버전을 올려야 한다 | **이벤트 계약 담당** |
| 내부 연결 Evidence | `script_interpreter_external_connection` 은 전역 IP 만 인정해 내부 연결은 Evidence 가 되지 않는다 | 역할 2 |
| R1 Fusion profile | `configs/fusion/fusion_config_v0.1.yaml` 은 `s0-profile` 과 S0 Evidence 만 포함. R1 profile 과 Fusion 실행 조건이 필요 | 역할 1 — 역할 2 의 계보 Evidence 정의, 역할 5 의 평가 조건과 연계 |
| 비교 평가 조건 | 평가 horizon, 비교 조건, 판정 기준 (§8-4) | 역할 5 |
| Hybrid 정책 | R1 병렬 실행에서 Fast · Fusion 결과를 결합하는 정책 | 역할 1 · 5 공동 합의 (`docs/schema/result-contracts.md`) |
| 원격 실행 runner | 현재 S0 runner 는 로컬 실행만 한다. 원격 세션 수립과 Target-A 수집이 새로 필요 | 역할 4 — issue #73 |
| S0 runner 병합 | R1 runner 는 S0 의 공통 함수를 재사용할 예정이라 PR #94 병합 뒤 착수 | 역할 4 |
| AWS 실제 사이클 증적 | R1 실제 사이클은 팀 AWS 환경에서 실행한다. Run 마다 입력 데이터, 실행 설정과 config version, 코드 commit SHA, 파일 SHA-256, 실행 시각, 출력 결과, 비교 평가 결과를 보존한다. 배포 절차는 `docs/aws-development.md` 를 따른다 | 역할 4 — 수집 · 재현성 기록 |

**이 의존성 때문에 현재 단계에서는 §8-2 의 telemetry Pilot 까지만 가능하다.** Pilot 은 원본
JSONL 로 연결 · 계보를 검증하지만, 같은 검증을 정규화 · Evidence 경로로 재현하는 §8-3 Full 은
ProcessGuid 계약과 계보 Evidence 가 정해지기 전까지 `blocked` 이다. Fusion Score · Stopping 경로와
R1 비교는 Full 과 별도로 R1 Fusion profile 과 §8-4 의 비교 평가 진입 조건이 갖춰진 뒤에 다룬다.

---

## 11. 미결 사항

| # | 항목 | 필요한 답 | 확인 대상 |
| --- | --- | --- | --- |
| 1 | WinRM 목적 포트 | 5985 / 5986 | 역할 3 |
| 2 | 실험망 CIDR | 내부 연결 판정의 필수 조건 | 역할 3 |
| 3 | Controller · Target-A · Target-B 이름과 기준 snapshot | VM 식별자 | 역할 3 |
| 4 | observation horizon | R1 의 `evaluation_horizon_sec` | 역할 5 |
| 5 | reference action 확정 | t+0 세션 수립 기준 여부 | 역할 5 · 2 |
| 6 | 관리 도구 · 승인 wrapper · 중간 프로세스 | 무해한 승인 도구 선정 | 역할 2 |
| 7 | 원격 세션 host 프로세스 이름 | `wsmprovhost.exe` 가정 검증 | rehearsal telemetry |
| 8 | 계보 `evidence_type` 과 최소 필드 | §10 | 역할 2 — issue #85 |
| 9 | ProcessGuid 계약 확장 여부와 시점 | §10 | 이벤트 계약 담당 |
| 10 | t+8 연결의 승인 목적지 IP · 포트 | §8-1 S-5 · S-6 | 역할 3 |
| 11 | t+5 · t+8 판정 시간 구간 | §8-1 S-2 · S-7 | 역할 4 · 5 |

---

## 12. 참조

```text
docs/scenarios/s0.md                              S0 형식, §10 R1 승격 시 달라지는 것
docs/data-contract-v0.2.md                        §2 버전 원칙, §5-4 host 귀속
docs/schema/event-v0.md                           §7 process 구조
docs/schema/run-id.md                             run_id / run_type / scenario_id
src/incident_awareness/common/models/event.py     ProcessInfo
src/incident_awareness/normalization/sysmon.py    EID 1 · 3 정규화
configs/sysmon/sysmonconfig-sample-v0.1.xml       EID 1 · 3 기록 범위
configs/evidence_types_v0.2.yaml                  Evidence 유형
configs/fusion/fusion_config_v0.1.yaml            Fusion profile
samples/raw/sysmon-0001.jsonl                     EID 1 · 3 원천 필드 확인
docs/schema/result-contracts.md                   FusionResult 역할 1, DecisionResult 역할 1·5
docs/roles/role5-fast-detector-policy.md          comparator 동결 lifecycle
docs/aws-development.md                           AWS 실제 사이클 배포 절차
issue #44 · #87                                    decisive comparator 후보 · 단계별 동결
issue #72                                          R1 시나리오 정의
issue #73                                          R1 실행기
issue #85                                          R1 Evidence 후보
```
